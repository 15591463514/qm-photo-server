## todoList

1. 服务端没有接口鉴权 ✅
2. 服务器安全问题
   1. helmet
   2. csrf-csrf
   3. @nestjs/throttler ✅
3. 微信扫码登录功能
4. 通知管理（system）
   1. 邮箱
   2. 微信订阅消息（https://blog.csdn.net/m0_60016849/article/details/144877758）
   3. 企业微信（暂不考虑）
   4. 飞书（暂不考虑）
   5. 短信（1分钱1条，暂不考虑）

## feat

1. 用户管理页面修改搜索的查询条件 ✅
   1. 需要增加搜索条件：角色
   2. 暂时在前端删除邮箱字段；
   3. 用户名和昵称使用一个输入框，在后端接口进行两者的模糊查询
      1. label：名称，placeholder：请输入用户名/昵称
   4. 增加搜索条件：注册日期（范围选择，查询的是创建日期）
   5. 最后的顺序为：名称、角色、状态、性别、注册日期
2. 代码组件拆分
   1. ts定义拆分，api.ts内，ts定义太多，按模块拆分下文件
   2. api接口函数过多，请拆分
3. 规范日志
   1. 对于401, 403等日志，不需要记录
4. 双token认证
   1. 前端完成剩余部分

## bug

1. ~~新注册的账号刚进来是「抱歉，您访问的页面不存在」，且接口报错资源不存在~~
   ~~1. 期望：新注册的用户默认是user（普通用户角色）~~ ✅ 已修复
2. ~~分配权限后，进入页面显示，加载字典无权限报错，并且报错两个弹窗「加载字典数据失败」，「禁止访问该资源」~~ ✅ 已修复
3. ~~给某个角色绑定菜单权限，如果只勾选了二级菜单，不勾选一级。则该角色用户进入页面后，菜单栏缺少一级，直接可以展示二级菜单。期望：~~
   ~~1. 在绑定角色的时候如果勾选了二级菜单，则主动勾选父级菜单~~
   ~~2. 如果取消勾选父级，子菜单也取消勾选；但是主动勾选，则子集不用主动勾选~~
   ~~3. 菜单和行内的按钮权限不需要进行联动~~ ✅ 已修复
4. ~~增加部分场景的表单校验（公共的验证请抽离验证正则）~~ ✅ 已修复
   ~~1. 注册的时候，账号填写input红色提示报错没有中文~~
   ~~2. 登录的时候，账号填写的限制缺少，应该和注册的限制一样~~
   ~~3. 新增编辑用户的时候，昵称不能输入空格，期望输入内容为16个字符以内（前端&后端）~~
   ~~4. 角色编码只能是小写字母（前端&后端），最大16字符~~
   ~~5. 字典类型名称和字典标签只不可输入特殊字符和emoji，最大16字符~~
   ~~6. 字典类型编码和字典值只能是字母,短横线,下划线,数字，最大16字符~~
   ~~7. 标签样式，字母,短横线,下划线,数字，最大16字符~~
   ~~8. 按照类似的校验规则，请修改权限弹窗内表单的验证~~
5. ~~当只勾选按钮权限，不勾选菜单权限，菜单还是会展示出来~~ ✅ 已修复
6. ~~分配权限，当父菜单下的所有子菜单都没有勾选，父菜单也应该不勾选；~~ ✅ 已修复
7. ~~很多场景下，按钮可以重复点击，例如查询等场景，期望接口调用加入防抖操作~~ ✅ 已修复
8. ~~字典get应该是公共接口，不需要鉴权和权限；~~ ✅ 已修复
9. ~~权限标识字段可以使用:符号~~ ✅ 已修复
10. ~~快速操作页面提示操作频繁，期望后端修改对同一个接口不能连续调用；对于速率配置稍微加大一点点限制~~ ✅ 已修复
