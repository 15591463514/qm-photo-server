# 公共配置
# Common Configuration (shared by all environments)
# 
# 注意：此文件只包含非敏感的公共配置
# 敏感信息（密码、密钥等）请放在 .env.local 中

# 【应用配置】
# 应用端口
PORT=3008

# 【数据库配置】
# 数据库主机地址
DB_HOST=localhost
# 数据库用户名
DB_USERNAME=dawn
# 数据库端口
DB_PORT=3306
# 注意：数据库密码请放在 .env.local 中
# DB_PASSWORD=your_db_password
# 注意：数据库名称请放在 .env.local 中
# DB_DATABASE=your_database_name

# 【JWT配置】
# JWT Token 过期时间
JWT_EXPIRES_IN=7d

# 【Redis配置】
# Redis 主机地址
REDIS_HOST=localhost
# Redis 端口
REDIS_PORT=6379
# Redis 数据库编号
REDIS_DB=0
# Redis 键前缀
REDIS_KEY_PREFIX=qm_photo:

# 【邮件配置】
# SMTP 服务类型（可选，如：QQ、Gmail，如果设置了则不需要配置 host 和 port）
SMTP_SERVICE=QQ
# SMTP 服务器地址（如果未设置 SMTP_SERVICE，则需要配置）
SMTP_SERVER=smtp.qq.com
# SMTP 端口（465 为 SSL，587 为 TLS，如果设置了 SMTP_SERVICE 则不需要）
SMTP_PORT=465
# 是否使用 SSL/TLS（当端口为 465 时自动为 true）
SMTP_SECURE=true
# 注意：SMTP 用户名、密码等敏感信息请放在 .env.local 中
# SMTP_USERNAME=your_email@example.com
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=your_email@example.com
# 发件人名称
SMTP_FROM_NAME=系统通知

# 【OSS配置】
# OSS 地区
OSS_REGION=oss-cn-chengdu
# 注意：OSS AccessKey ID、Secret 等敏感信息请放在 .env.local 中
# OSS_ACCESS_KEY_ID=your_oss_access_key_id
# OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret
# OSS_BUCKET=your_bucket_name
# 开启内网端点
OSS_INTERNAL=true
# OSS Endpoint
OSS_ENDPOINT=https://oss-cn-chengdu.aliyuncs.com
# 是否开启 HTTPS
OSS_SECURE=true
# 前缀路径
OSS_PREFIX=qm-photo/dev/
# 图片存储路径前缀
OSS_IMAGE_PREFIX=images/
# 音频存储路径前缀
OSS_AUDIO_PREFIX=audio/
# 视频存储路径前缀
OSS_VIDEO_PREFIX=video/
# 注意：CDN 域名请放在 .env.local 中
# OSS_CDN_DOMAIN=your_cdn_domain
# 文件访问过期时间（永不过期）
OSS_EXPIRES=0
